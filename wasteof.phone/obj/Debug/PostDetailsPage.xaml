<Page
    x:Class="wasteof.phone.PostDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wasteof.phone"
    xmlns:models="using:wasteof.phone.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TitlePanel -->
        <StackPanel Grid.Row="0" Margin="19,19,0,12">
            <TextBlock Text="WASTEOF.PHONE" Style="{ThemeResource TitleTextBlockStyle}" Typography.Capitals="AllSmallCaps"/>
            <TextBlock Text="post" Margin="0,-6.5,0,0" Style="{ThemeResource HeaderTextBlockStyle}"/>
        </StackPanel>

        <!-- ContentPanel -->
        <ListView x:ConnectionId='1' x:Name="CommentsListView" Grid.Row="1" Margin="19,0,19,0" SelectionMode="None" IsItemClickEnabled="True"                                        HorizontalContentAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Header>
                <!-- Main Post Content -->
                <Border Margin="0,0,0,16" BorderBrush="{ThemeResource PhoneBorderBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Profile Picture -->
                        <Border x:ConnectionId='4' x:Name="PostProfilePictureBorder" Width="50" Height="50" VerticalAlignment="Top" Margin="0,0,12,0" Background="{ThemeResource PhoneBorderBrush}"                                    >
                            <Image x:Name="PostProfilePictureImage" Width="50" Height="50" Stretch="UniformToFill"/>
                        </Border>

                        <StackPanel Grid.Column="1">
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:ConnectionId='8' x:Name="PosterNameTextBlock" Text="poster" FontWeight="Bold" Style="{ThemeResource SubheaderTextBlockStyle}"                                    />
                                <TextBlock x:Name="PostTimeTextBlock" Grid.Column="1" Text="time" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource PhoneMutedBrush}" VerticalAlignment="Center"/>
                            </Grid>

                            <TextBlock x:Name="PostContentTextBlock" Text="post content goes here" TextWrapping="Wrap" Margin="0,0,0,12" Style="{ThemeResource MessageTextStyle}"/>

                            <!-- Repost layout inside if it exists -->
                            <Border x:Name="RepostBorder" BorderBrush="{ThemeResource PhoneAccentBrush}" BorderThickness="3,0,0,0" Padding="8,0,0,0" Margin="0,0,0,12" Visibility="Collapsed">
                                <StackPanel>
                                    <TextBlock x:Name="RepostPosterName" Text="reposted poster" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>
                                    <TextBlock x:Name="RepostContent" Text="reposted content" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}"/>
                                </StackPanel>
                            </Border>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <!-- Love Button -->
                                <Button x:ConnectionId='5' x:Name="LoveButton"                          Margin="0,0,12,0" Padding="16,8,16,8" MinHeight="44" Style="{StaticResource ButtonSimpleStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <FontIcon x:Name="LoveIcon" FontFamily="Segoe UI Symbol" Glyph="&#x2661;" Foreground="#888888" FontSize="16" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="LoveCountTextBlock" Text="0" Style="{ThemeResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <!-- Repost Button -->
                                <Button x:ConnectionId='6' x:Name="RepostButton"                            Margin="0,0,12,0" Padding="16,8,16,8" MinHeight="44" Style="{StaticResource ButtonSimpleStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <SymbolIcon Symbol="Sync" Foreground="{ThemeResource PhoneMutedBrush}" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="RepostCountTextBlock" Text="repost" Style="{ThemeResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <!-- Delete Button -->
                                <Button x:ConnectionId='7' x:Name="DeleteButton"                            Visibility="Collapsed" Foreground="Red" Padding="16,8,16,8" MinHeight="44" Style="{StaticResource ButtonSimpleStyle}">
                                    <TextBlock Text="delete" Style="{ThemeResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                </Button>
                            </StackPanel>

                            <TextBlock Text="comments" Margin="0,8,0,8" FontWeight="SemiBold" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource PhoneAccentBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </ListView.Header>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="{Binding CommentMargin}" HorizontalAlignment="Stretch" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Profile Picture -->
                        <Border x:ConnectionId='9' Width="32" Height="32" VerticalAlignment="Top" Margin="0,0,10,0" Background="{ThemeResource PhoneBorderBrush}"                              >
                            <Image Source="{Binding Poster.ProfilePictureUrl}" Width="32" Height="32" Stretch="UniformToFill"/>
                        </Border>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Stretch">
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <!-- Clickable commenter name -->
                                <TextBlock x:ConnectionId='10' Text="{Binding Poster.Name}" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource PhoneAccentBrush}"                              />
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBlock Text="{Binding FormattedTime}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource PhoneMutedBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Button x:ConnectionId='11' Content="reply"                                  Padding="12,0,12,0" MinHeight="44" MinWidth="64" FontSize="12" Style="{StaticResource ButtonSimpleStyle}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="{Binding CleanContent}" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- New Comment Panel -->
        <Border Grid.Row="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource PhoneBorderBrush}" Padding="19,8,19,8">
            <StackPanel>
                <!-- Reply-To Indicator -->
                <Grid x:ConnectionId='2' x:Name="ReplyToIndicator" Visibility="Collapsed" Margin="0,0,0,6"                                 >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ReplyToTextBlock" Text="replying to @username" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource PhoneAccentBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="✕" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource PhoneMutedBrush}" VerticalAlignment="Center" Padding="4"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="NewCommentTextBox" PlaceholderText="add a comment..." TextWrapping="Wrap" Margin="0,0,8,0" MaxLength="1000" VerticalAlignment="Center"/>
                    <Button x:ConnectionId='3' x:Name="SendCommentButton" Grid.Column="1" Content="send"                                 VerticalAlignment="Center" Margin="0"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Page>

