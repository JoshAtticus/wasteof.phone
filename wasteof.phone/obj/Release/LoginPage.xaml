<Page
    x:Class="wasteof.phone.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wasteof.phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TitlePanel -->
        <StackPanel Grid.Row="0" Margin="19,19,0,28">
            <TextBlock Text="WASTEOF.PHONE" Style="{ThemeResource TitleTextBlockStyle}" Typography.Capitals="AllSmallCaps"/>
            <TextBlock Text="sign in" Margin="0,-6.5,0,0" Style="{ThemeResource HeaderTextBlockStyle}"/>
        </StackPanel>

        <!-- ContentPanel -->
        <Grid Grid.Row="1" x:Name="ContentPanel" Margin="19,0,19,0">
            <StackPanel VerticalAlignment="Top">
                <TextBlock Text="username" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,8" />
                <TextBox x:Name="UsernameTextBox" Margin="0,0,0,16" PlaceholderText="enter your username"/>

                <TextBlock Text="password" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,8" />
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,24" PlaceholderText="enter your password"/>

                <Button x:ConnectionId='1' x:Name="LoginButton" Content="sign in" HorizontalAlignment="Stretch"                           Height="44" />

                <ProgressRing x:Name="LoginProgress" Margin="0,20,0,0" IsActive="False" Width="50" Height="50" HorizontalAlignment="Center"/>

                <TextBlock x:Name="ErrorTextBlock" Foreground="Red" TextWrapping="Wrap" Margin="0,16,0,0" HorizontalAlignment="Center" TextAlignment="Center" Style="{ThemeResource BodyTextBlockStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

