<Page
    x:Class="wasteof.phone.NotificationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wasteof.phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Pivot x:Name="NotificationsPivot" Grid.Row="1" Title="messages" SelectionChanged="NotificationsPivot_SelectionChanged">
            <PivotItem Header="unread">
                <Grid>
                    <ProgressBar x:Name="UnreadProgressBar" VerticalAlignment="Top" IsIndeterminate="True" Visibility="Collapsed"/>
                    <ListView x:Name="UnreadListView" IsItemClickEnabled="True" ItemClick="NotificationListView_ItemClick" HorizontalContentAlignment="Stretch">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,16" Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Width="40" Height="40" VerticalAlignment="Top" Margin="0,0,12,0" Background="{ThemeResource PhoneBorderBrush}" Tapped="Username_Tapped">
                                        <Image Source="{Binding Data.Actor.ProfilePictureUrl}" Width="40" Height="40" Stretch="UniformToFill"/>
                                    </Border>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding NotificationText}" TextWrapping="Wrap" FontWeight="SemiBold" Style="{ThemeResource BodyTextBlockStyle}"/>
                                        <TextBlock Text="{Binding NotificationContent}" Margin="0,2,0,0" TextWrapping="Wrap" MaxLines="2" TextTrimming="WordEllipsis" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{StaticResource PhoneMutedBrush}"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding FormattedTime}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{StaticResource PhoneMutedBrush}" VerticalAlignment="Top" Margin="8,0,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.Footer>
                            <Button x:Name="LoadMoreUnreadButton" Content="load more..." Click="LoadMoreUnreadButton_Click" HorizontalAlignment="Center" Margin="0,12,0,12" Style="{ThemeResource ButtonSimpleStyle}" Visibility="Collapsed"/>
                        </ListView.Footer>
                    </ListView>
                </Grid>
            </PivotItem>

            <PivotItem Header="read">
                <Grid>
                    <ProgressBar x:Name="ReadProgressBar" VerticalAlignment="Top" IsIndeterminate="True" Visibility="Collapsed"/>
                    <ListView x:Name="ReadListView" IsItemClickEnabled="True" ItemClick="NotificationListView_ItemClick" HorizontalContentAlignment="Stretch">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,16" Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Width="40" Height="40" VerticalAlignment="Top" Margin="0,0,12,0" Background="{ThemeResource PhoneBorderBrush}" Tapped="Username_Tapped">
                                        <Image Source="{Binding Data.Actor.ProfilePictureUrl}" Width="40" Height="40" Stretch="UniformToFill"/>
                                    </Border>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding NotificationText}" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.7"/>
                                        <TextBlock Text="{Binding NotificationContent}" Margin="0,2,0,0" TextWrapping="Wrap" MaxLines="2" TextTrimming="WordEllipsis" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{StaticResource PhoneMutedBrush}" Opacity="0.7"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding FormattedTime}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{StaticResource PhoneMutedBrush}" VerticalAlignment="Top" Margin="8,0,0,0" Opacity="0.7"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.Footer>
                            <Button x:Name="LoadMoreReadButton" Content="load more..." Click="LoadMoreReadButton_Click" HorizontalAlignment="Center" Margin="0,12,0,12" Style="{ThemeResource ButtonSimpleStyle}" Visibility="Collapsed"/>
                        </ListView.Footer>
                    </ListView>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="MarkAllReadButton" Icon="Accept" Label="mark all read" Click="MarkAllReadButton_Click"/>
            <AppBarButton x:Name="RefreshButton" Icon="Refresh" Label="refresh" Click="RefreshButton_Click"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
