<Page
    x:Class="wasteof.phone.PostDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wasteof.phone"
    xmlns:models="using:wasteof.phone.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="19,19,0,12">
            <TextBlock Text="WASTEOF.PHONE" Style="{ThemeResource TitleTextBlockStyle}" Typography.Capitals="AllSmallCaps"/>
            <TextBlock Text="post" Margin="0,-6.5,0,0" Style="{ThemeResource HeaderTextBlockStyle}"/>
        </StackPanel>

        <ListView x:Name="CommentsListView" Grid.Row="1" Margin="19,0,19,0" SelectionMode="None" IsItemClickEnabled="True" ItemClick="CommentsListView_ItemClick" HorizontalContentAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Header>
                <Border Margin="0,0,0,16" BorderBrush="{ThemeResource PhoneBorderBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border x:Name="PostProfilePictureBorder" Width="50" Height="50" VerticalAlignment="Top" Margin="0,0,12,0" Background="{ThemeResource PhoneBorderBrush}" Tapped="PosterNameTextBlock_Tapped">
                            <Image x:Name="PostProfilePictureImage" Width="50" Height="50" Stretch="UniformToFill"/>
                        </Border>

                        <StackPanel Grid.Column="1">
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="PosterNameTextBlock" Text="poster" FontWeight="Bold" Style="{ThemeResource SubheaderTextBlockStyle}" Tapped="PosterNameTextBlock_Tapped"/>
                                <TextBlock x:Name="PostTimeTextBlock" Grid.Column="1" Text="time" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource PhoneMutedBrush}" VerticalAlignment="Center"/>
                            </Grid>

                            <RichTextBlock x:Name="PostContentTextBlock" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,12"/>

                            <Image x:Name="PostImage" MaxHeight="400" Stretch="Uniform" HorizontalAlignment="Left" Margin="0,0,0,12" Visibility="Collapsed"/>

                            <Border x:Name="RepostBorder" BorderBrush="{ThemeResource PhoneAccentBrush}" BorderThickness="3,0,0,0" Padding="8,0,0,0" Margin="0,0,0,12" Visibility="Collapsed">
                                <StackPanel>
                                    <TextBlock x:Name="RepostPosterName" Text="reposted poster" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>
                                    <RichTextBlock x:Name="RepostContent" FontSize="15" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <Button x:Name="LoveButton" Click="LoveButton_Click" Margin="0,0,12,0" Padding="16,8,16,8" MinHeight="44" Style="{StaticResource ButtonSimpleStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <Path x:Name="LoveIcon" Data="M 12,5 C 10,3 6,3 4,5 C 2,7 2,11 4,13 L 12,21 L 20,13 C 22,11 22,7 20,5 C 18,3 14,3 12,5 Z" Stroke="#888888" StrokeThickness="2" Fill="Transparent" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="LoveCountTextBlock" Text="0" Style="{ThemeResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <Button x:Name="RepostButton" Click="RepostButton_Click" Margin="0,0,12,0" Padding="16,8,16,8" MinHeight="44" Style="{StaticResource ButtonSimpleStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="M 4,12 L 4,7 C 4,5 5,4 7,4 L 17,4 M 13,1 L 18,4 L 13,7 M 16,8 L 16,13 C 16,15 15,16 13,16 L 3,16 M 7,13 L 2,16 L 7,19" Stroke="{ThemeResource PhoneMutedBrush}" StrokeThickness="2" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0">
                                            <Path.RenderTransform>
                                                <RotateTransform Angle="-30" CenterX="9" CenterY="9"/>
                                            </Path.RenderTransform>
                                        </Path>
                                        <TextBlock x:Name="RepostCountTextBlock" Text="repost" Style="{ThemeResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <Button x:Name="DeleteButton" Click="DeleteButton_Click" Visibility="Collapsed" Foreground="Red" Padding="16,8,16,8" MinHeight="44" Style="{StaticResource ButtonSimpleStyle}">
                                    <TextBlock Text="delete" Style="{ThemeResource BodyTextBlockStyle}" VerticalAlignment="Center"/>
                                </Button>
                            </StackPanel>

                            <TextBlock Text="comments" Margin="0,8,0,8" FontWeight="SemiBold" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource PhoneAccentBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </ListView.Header>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="{Binding CommentMargin}" HorizontalAlignment="Stretch" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Width="32" Height="32" VerticalAlignment="Top" Margin="0,0,10,0" Background="{ThemeResource PhoneBorderBrush}" Tapped="CommenterName_Tapped">
                            <Image Source="{Binding Poster.ProfilePictureUrl}" Width="32" Height="32" Stretch="UniformToFill"/>
                        </Border>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Stretch">
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Poster.Name}" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource PhoneAccentBrush}" Tapped="CommenterName_Tapped"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBlock Text="{Binding FormattedTime}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource PhoneMutedBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Button Content="reply" Click="ReplyCommentButton_Click" Padding="12,0,12,0" MinHeight="44" MinWidth="64" FontSize="12" Style="{StaticResource ButtonSimpleStyle}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="{Binding CleanContent}" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Row="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource PhoneBorderBrush}" Padding="19,8,19,8">
            <StackPanel>
                <Grid x:Name="ReplyToIndicator" Visibility="Collapsed" Margin="0,0,0,6" Tapped="ReplyToIndicator_Tapped">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ReplyToTextBlock" Text="replying to @username" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource PhoneAccentBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="✕" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource PhoneMutedBrush}" VerticalAlignment="Center" Padding="4"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="NewCommentTextBox" PlaceholderText="add a comment..." TextWrapping="Wrap" Margin="0,0,8,0" MaxLength="1000" VerticalAlignment="Center"/>
                    <Button x:Name="SendCommentButton" Grid.Column="1" Content="send" Click="SendCommentButton_Click" VerticalAlignment="Center" Margin="0"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
